pipeline {
    agent any

    stages {
		// Checkout
        stage('Get Latest Code') {
			steps {
		        // Add steps to clone the repository
                echo 'Get Latest Code'
                git url: 'https://github.com/mmukul/simple-app-devopscicd-demo.git'
			}
		}
    }
}